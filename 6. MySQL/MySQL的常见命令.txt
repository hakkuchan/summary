查看所有数据库：show databases;

打开指定的数据库：use 库名;

查看当前数据库的所有表：show tables;

查看其它库的所有表：show tables from 库名;

创建表：
create table 表名(
    列名 列类型，
	列名 列类型，
	……);
	
查看表结构：desc 表名;

查看服务器版本：
方式一：select version();
方式二：命令行 mysql --version